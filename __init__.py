# Initialise the application package.

"""
The ``app`` package houses all code for the FastAPI service.  It exposes
submodules for routing, data models and business logic.  Importing the
package has no side effects; to create the application instance, see
``app.main.app``.
"""
